/** Accessibility checks panel — stub while the bench is mid-build by its owner. */
export function ChecksPanel() {
  return <div className="panel"><p className="dim">Checks — wiring up…</p></div>;
}

export default ChecksPanel;
