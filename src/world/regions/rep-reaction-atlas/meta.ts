/**
 * Rep Reaction Atlas — demo meta. Follows the demo sheet format
 * ({ title, description, tags, client, caseStudy }); `blurb` feeds lab cards.
 */
export const meta = {
  title: 'Rep Reaction Atlas',
  description:
    'A dispatch-office card table for the reputation system. Scrub Salt Guild ◆, Choir ◉ and Reclaimer ▲ standing from hostile to kin on amber needle gauges, then run a seeded courier around a scripted ten-band survey ride while the bench restages the exact shipping radio pipeline — band detection, weight rules, voice picks and subtitle lines, live from the character library. Three faction-coloured ledger columns break down which surfaces answer to standing (radio airtime) and which stay silent (what they say, NPC callouts, board gates, shop prices); a coverage matrix read straight from the live mailing lists marks every cell shipped / runtime-ready / gap with glyph + colour, and flags the findings — the unreachable hostile band, ghost driftline rep, the workshed sandbox leaking its own radio band — with a copy-ready claim sheet for the writers.',
  blurb:
    'Scrub faction standing from hostile to kin and watch the desert change its voice: a scripted ride restages the exact radio/bark pipeline, gauges show what ships at each band, and the coverage matrix flags the gaps writers can claim.',
  tags: ['tool', 'reputation', 'radio', 'factions', 'content-coverage'],
  client: 'Driftline dispatch office',
  caseStudy:
    'Reputation had three ways to matter on paper — radio weighting, board gates, shop prices — but only one shipped, and nobody could see it. The Atlas restages the shipping pipeline honestly: the band rule, the weight formula and the chatter cadence are mirrored line-for-line from the HUD, fed by the live character and mission libraries with no fixtures. Sliders stand in for standing; a scripted courier loops every on-world band so each faction is heard at home and abroad. Needle gauges tag every standing with its posture band (hostile/wary/neutral/friendly/kin, matching the ledger bench’s rail proposal), and a coverage matrix separates shipped behaviour from runtime-ready hooks from true gaps — shape + colour, never colour alone. The export button copies the whole sheet as a claim list for writers.',
};

export default meta;
