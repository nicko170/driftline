/**
 * Skybox & Day Cycle Tuner — demo meta. Follows the demo sheet format
 * ({ title, description, tags, client, caseStudy }); `blurb` feeds lab-style cards.
 */
export const meta = {
  title: 'Skybox & Day Cycle Tuner',
  description:
    'Design tool: scrub the DRIFTLINE day/night cycle across its palette keyframes over a bare dune vista. Tune fog density, sun intensity and star factor live, then export the tuned settings as JSON.',
  blurb:
    'Scrub dawn → day → dusk → night over a bare dune vista. Fog, sun and star sliders; copies the tuned palette as JSON.',
  tags: ['tool', 'palette', 'lighting', 'r3f'],
  client: 'Driftline art direction',
  caseStudy:
    'Dial in the sun-bleached day / violet + neon-amber night palette from DESIGN.md without rebuilding the game. The exported JSON mirrors the keyframe rows of src/game/Sky.tsx.',
};
